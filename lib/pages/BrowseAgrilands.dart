import 'package:flutter/material.dart';

class BrowseAgriLandsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Browse Agri-Lands'),
      ),
      body: Center(
        child: Text('Browse Agri-Lands Page'),
      ),
    );
  }
}