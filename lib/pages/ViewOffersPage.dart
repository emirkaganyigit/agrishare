import 'package:flutter/material.dart';

class ViewOffersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('View Offers'),
      ),
      body: Center(
        child: Text('View Offers Page'),
      ),
    );
  }
}